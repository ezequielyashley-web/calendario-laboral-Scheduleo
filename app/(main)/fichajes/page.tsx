import FichajesDesktop from "@/components/desktop/FichajesDesktop"

export default function FichajesPage() {
  return (
    <div style={{ background: "rgba(255,255,255,0.92)", borderRadius: 20, padding: 24, boxShadow: "0 8px 32px rgba(0,0,0,0.08)" }}>
      <FichajesDesktop />
    </div>
  )
}